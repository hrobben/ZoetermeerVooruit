{% extends '@Main/base.html.twig' %}

{% block body %}
    <h1>Raadswerk</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ raadswerk.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ raadswerk.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ raadswerk.description }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('raadswerk_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('raadswerk_edit', { 'id': raadswerk.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
