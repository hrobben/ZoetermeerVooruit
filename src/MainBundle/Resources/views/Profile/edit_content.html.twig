{% trans_default_domain 'FOSUserBundle' %}

{{ form_start(form, { 'action': path('fos_user_profile_edit'), 'attr': { 'class': 'fos_user_profile_edit' } }) }}
    {{ form_widget(form) }}
    <div>
        <input class="btn btn-lg btn-success" type="submit" value="Opslaan" />
        <a href="{{ path('fos_user_profile_show') }}" class="btn btn-lg btn-primary">Annuleren</a>
    </div>
{{ form_end(form) }}
