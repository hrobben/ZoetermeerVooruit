{% extends '@Main/base.html.twig' %}

{% block body %}
    <h1>Steun</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ steun.id }}</td>
            </tr>
            <tr>
                <th>Titel</th>
                <td>{{ steun.title }}</td>
            </tr>
            <tr>
                <th>Beschrijving</th>
                <td>{{ steun.description }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('steun_index') }}">Terug naar overzicht</a>
        </li>
        <li>
            <a href="{{ path('steun_edit', { 'id': steun.id }) }}">Aanpassen</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
